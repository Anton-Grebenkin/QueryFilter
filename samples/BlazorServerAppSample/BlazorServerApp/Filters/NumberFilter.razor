@namespace BlazorServerApp.Filters
@using QueryFilter.Models;
@typeparam TableItem

@if (Column.FilterControl == this)
{
    <select class="form-control form-control-sm" value="@Condition" @onchange="@((x) => Condition = (ExpressionOperatorType)Enum.Parse(typeof(ExpressionOperatorType), x.Value.ToString()))">
        @foreach (var option in (ExpressionOperatorType[])Enum.GetValues(typeof(ExpressionOperatorType)))
        {
            <option value="@option">@option.ToString()</option>
        }
    </select>

    <input type="number" class="form-control form-control-sm" @bind-value="FilterValue" />

}